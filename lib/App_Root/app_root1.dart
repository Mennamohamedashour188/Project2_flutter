import 'package:flutter/material.dart';
import '../Screens/Home1.dart';
class task_one extends StatelessWidget {
  const task_one({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home:HOME_ONE(),);
  }
}
